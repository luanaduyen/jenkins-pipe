pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh 'echo "Oi para o nosso Jenkins."'
                sh '''
                    echo "Nossa primeira pipeline."
                    la -la
                '''
            }
        }
    }
}
